# The Close Reading Program — demo

A self-contained prototype demo page (single `index.html`). Not an offer of enrollment;
sample feedback shown is illustrative prototype output. No data is collected.
